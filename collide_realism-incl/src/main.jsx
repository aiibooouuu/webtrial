import { createRoot } from 'react-dom/client';
import App from './App.jsx';
import './index.css'; // Make sure to create this file or adjust the path if necessary

// Render the app
createRoot(document.getElementById('root')).render(<App />);
